# ChatPulse 執行依賴
#
# 版本已鎖定（SPECIFICATION.md 第十三節的驗收條件：乾淨環境可用
# `pip install -r requirements.txt` 重建）。版本取自本專案 .venv（Python 3.12）
# 的實際安裝結果，2026-09-05 產出。
#
# 重建方式：
#   uv venv --python 3.12 .venv
#   uv pip install -r requirements.txt
#
# 執行前必須設定環境變數 GOOGLE_API_KEY，否則啟動會失敗並提示。

# --- 共用（core）---
google-api-python-client==2.200.0
google-auth-oauthlib==1.4.1
google-auth-httplib2==0.4.2
requests==2.34.2
# core/crypto.py 用 Fernet 對稱加密存進 credentials 表的 OAuth token
cryptography==50.0.1

# --- MCP server（mcp_app）---
mcp==2.1.1

# --- AI 供應商（core/providers）---
# 這裡沒有列任何 AI SDK，是刻意的：gemini 供應商用 requests 直接打 REST，
# claude_cli 供應商靠本機的 Claude Code 執行檔。2026-09-05 移除 claude_api
# 供應商後，anthropic 套件一併移除。

# --- 訊息圖片（core/attachments.py）---
# 送圖給模型前一律縮圖。實測成本與像素數成正比（約 750 像素／token）且
# API 不會替你縮，1920x1080 一張就要 2,694 tokens；縮到長邊 1024 省約 61%。
Pillow==12.3.0

# --- 儀表板後端（dashboard）---
fastapi==0.141.1
# [standard] 額外帶入 uvloop／httptools／watchfiles／websockets，SSE 與 --reload 需要
uvicorn[standard]==0.52.4
